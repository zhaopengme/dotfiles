# Private Homebrew (CLI) packages
#
# Keep this list restricted to private-only tools:
# - secrets / crypto tooling
# - company-specific CLIs
# - anything you don't want in the public dotfiles
#
# Install via your private installer script:
#   brew install $(grep -vE '^\s*#|^\s*$' brew/cli.private.txt)

# Secrets management
git-secret
gnupg

# Optional: secret storage / SSO helpers (uncomment if you use them)
# age
# sops
# 1password-cli

# Company / ops tooling (examples)
# awscli
# terraform
# kubectl
# helm
# vault

# Networking / debugging (examples)
# mitmproxy
# openssl@3
# socat
